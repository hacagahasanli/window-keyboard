import { Keyboard } from "components/Keyboard"
import { FC } from "react"

const Home: FC = () => {
    return <main>
        {/* <TextArea /> */}
        <Keyboard />
    </main>
}

export default Home